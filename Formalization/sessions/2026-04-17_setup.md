# 2026-04-17 — Cowork migration & workspace setup

**What.** Andrew asked "let's set up the project to your liking." Clarified goals: set up as a project-lead environment, full autonomy on direction, markdown-only, no `.docx` outputs, migrate memory into Cowork's system.

## What I did

1. **Inventoried the existing corpus** (delegated to a general-purpose subagent to keep context light). Found 30+ markdown docs, one PDF, one `memory.md`, one `metadata.json` under a flat `docs/` + `files/` layout. Active paper was `docs/acp_physics_paper_v10.md`, internal masthead "WORKING DRAFT — v0.9".

2. **Designed a topic-based folder structure.** Chose semantics over flatness because the corpus has clear internal structure (active paper vs. proofs vs. domain reductions vs. structural bridges vs. philosophical essays). Full layout documented in `CLAUDE.md`.

3. **Reorganized files.**
   - `paper/acp_main_v10.md` — one active paper, version suffix dropped
   - `proofs/` — five core proof documents, version suffixes dropped
   - `reductions/` — seven mapping documents (Prigogine, Kauffman, Friston, Zurek, Bergstrom–Lachmann, Price/Fisher, multiscale RG)
   - `bridges/` — six structural / supporting appendices (Schur, syndrome coordination, coordination neutrality, restraint-power, non-Gaussian bounds, empirical predictions)
   - `special_cases/` — v03 of the extended catalog; v01–v02 archived
   - `essays/` — five companion essays, renamed to clean filenames
   - `audits/` — v07 integrity audit (flagged stale)
   - `references/` — the UUID-named PDF, renamed to `odrzywolek_2026_eml_operator.pdf` (arXiv:2603.21852v2 — the `eml(x,y) = exp(x) − ln(y)` universal-operator paper, directly relevant to `bridges/coordination_neutrality.md`)
   - `archive/paper_versions/` — v01 through v09 of the paper, including the duplicate `Acp physics paper v03 ·.md` cleaned up as `acp_physics_paper_v03.md`
   - `archive/special_cases_prior/` — v01 and v02 of the special cases catalog

   Empty `docs/` and `files/` folders couldn't be removed (Cowork mount permission), so left a `_MOVED.md` in each pointing to the new layout.

4. **Built the living-document layer at the root.**
   - `CLAUDE.md` — operating charter (Claude as lead, conventions, folder layout, session-startup routine, don'ts)
   - `STATUS.md` — current paper state, active fronts, full result inventory through v10, changelog starting today
   - `OPEN_PROBLEMS.md` — nine open problems with IDs (OP-1 through OP-9), including the re-audit of v10 and the Heisenberg/A.20 reconciliation
   - `README.md` — light human-facing overview

5. **Ported memory to the Cowork two-tier system.** Six entries written:
   - `andrew_role.md` (user) — how Andrew operates on this project
   - `acp_framework_overview.md` (project) — 30-second framework summary
   - `acp_paper_state_v10.md` (project) — what v10 contains, what's blocking journal prep
   - `workspace_conventions.md` (feedback) — markdown-only, file layout rules
   - `acp_technical_principles.md` (feedback) — six non-obvious technical rules (Gaussian as floor, equilibrium = dissolution, CDT self-grounding, etc.)
   - `formalization_paths.md` (reference) — where all the files live
   - plus the `MEMORY.md` index

   Legacy `memory.md` is preserved in the workspace with a warning header on top — it's a stale snapshot (asserts v09 is current, doesn't know about A.20) but kept for audit.

## What I noticed while doing it

- **Memory drift.** `memory.md` claims v09 is current and names 15 result categories. v10 exists and includes A.20 (Restraint-Power Theorem with Heisenberg as special case) — a result the legacy memory doesn't mention. This is exactly the kind of quiet drift that the migration to Cowork memory is supposed to fix.

- **Filename / masthead drift.** The file is `acp_physics_paper_v10.md` but the masthead reads "WORKING DRAFT — v0.9". Either bump the masthead to 1.0 during the next audit pass or rename the file to v09. Not urgent, but worth reconciling before journal prep.

- **Integrity audit is stale.** `audits/integrity_audit_v07.md` was run against paper v07. Three substantial additions have landed since (A.17 non-Gaussian bounds, A.18 multiscale RG, A.19 Price/Fisher, A.20 Restraint-Power/Heisenberg). The v07 findings — numbering gaps at Lemma 4.15 and 4.18, ε/ε* inconsistency, σ and T notation overloading — should be checked against v10. Filed as OP-8.

- **Schur bridge vs. A.20.** The schur-bridge doc still frames the Heisenberg derivation as an open problem, but A.20 seems to deliver it. Filed as OP-6 — needs a reconciliation note, and if A.20 fully closes it, the schur-bridge doc should be updated and OP-6 moved to Resolved.

## What I didn't do (deferred)

- **I didn't run the v10 integrity audit.** Setup only; the audit itself is the next substantive piece of research work. Named as the top active front in `STATUS.md`.
- **I didn't pick a journal.** Not ready to — depends on the audit outcome and how we pitch the paper.
- **I didn't scope the Tier-1 computational tests.** Named as OP-9.
- **I didn't edit the paper itself.** Zero edits to `paper/acp_main_v10.md` or any of the core content files — this was reorganization only.

## Open questions / anything to flag Andrew on

Nothing that needs his input. The setup is Claude-facing; he asked for that explicitly. The only choices that benefited from his input (scope of setup; file handling aggressiveness; memory porting) were resolved at the top of this session.

## Next session

Read `STATUS.md`, pick OP-8 (v10 integrity re-audit). That's the highest-value unblocking step.
